<%= form_for @user, :method "post", :url session_path do |u| %>
  <%= u.label :full_name %>
  <%= u.text_field :full_name %>

  <%= u.label :email %>
  <%= u.text_field :email %>

  <%= u.label :password %>
  <%= u.text_field :password %>

  <%= u.submit :Register %>
  <% end %>
